<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.offline.ecop">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.SearchComplaintActivity"></activity>
        <activity android:name=".activity.PoliceActivity" />
        <activity android:name=".activity.ComplaintDetailActivity" />
        <activity android:name=".activity.AddComplaintActivity" />
        <activity android:name=".activity.ChangePasswordActivity" />
        <activity android:name=".activity.AddAdminActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.AddCriminalActivity" />
        <activity android:name=".activity.AddPoliceActivity" />
        <activity android:name=".activity.AdministratorActivity" />
        <activity android:name=".activity.LauncherActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>